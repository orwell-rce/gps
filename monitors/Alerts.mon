package com.progress.demo.rce.gps;
using com.progress.demo.rce.gps.output.AlertPOI;

monitor alerts{
	
	AlertPOI alert;
	
	action onload() {
		on all AlertPOI():alert{
			print alert.pos.id.toString() + " inside of " + alert.poi.name;
		}
	}
}

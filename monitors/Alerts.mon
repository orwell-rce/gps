package com.progress.demo.rce.gps;
using com.progress.demo.rce.gps.output.Alert;

monitor alerts{
	
	Alert alert;
	
	action onload() {
		on all Alert():alert{
			print alert.pos.id.toString() + " inside of " + alert.poi.name;
		}
	}
}
